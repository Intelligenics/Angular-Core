// Angular
import { RouterModule } from '@angular/router';  
import { StylesRoutes } from '../../../module/src/lib/styles.routing';

 


export const AppRouting = RouterModule.forRoot(StylesRoutes,
    {
        // enableTracing: true
    });

